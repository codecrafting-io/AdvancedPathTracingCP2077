local defaults = {
    version = '0.2.0',
    debug = false,
	enableNRDControl = true,
    rayNumber = 2,
    rayBounce = 2,
	fastTimeout = 1.0,
    slowTimeout = 30.0,
    refreshGame = false,
    refreshTimeout = 5.0,
    enableDLSSDParticles = true,
    selfReflection = false,
    ptModeIndex = 2,
    ptQualityIndex = 3,
    ptOptimizations = true
}

return defaults
